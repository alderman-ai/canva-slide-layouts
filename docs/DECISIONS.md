# Decision log

Numbered, dated, with the reason and what it rules out. Append; never rewrite history. Reverse a decision with a new entry that cites the old number.

| # | Date | Decision | Reason | Rules out |
|---|---|---|---|---|
| 1 | 2026-09-02 | **No PPTX or other vendor design files in the repo.** `.gitignore` blocks `*.pptx *.ppt *.potx *.ppsx *.key *.odp *.psd *.ai *.sketch *.fig *.indd`; `validate` fails if any exist. | Operator rule: the directory holds markup and hybrid MD only; vendor files may exist only transitorily in the scratchpad during a conversion | PPTX as a hosted import artifact; the "PPTX primary" recommendation in research/07 |
| 2 | 2026-09-02 | **Hybrid Markdown is the operator surface.** One MD per layout, slide, unit set, deck, intake, bundle; **flat frontmatter only** (scalars and flat lists, no maps); structured per-element data in body tables. | Operator works from PowerShell and Obsidian; Obsidian has six property types and no enum, so vocab enforcement is external | nested YAML; JSON-only sources of truth |
| 3 | 2026-09-02 | **Upload routes.** Route C (masters + `merge-designs` + fill by locator) is the default for filled decks; Route A (annotated HTML import) builds masters and previews; Route B (`edit-design` ops) is for elements and repairs only; Route D (`.dc.html` → Claude Design → Send to Canva) is probed as the officially supported HTML path; transitory PPTX is emergency only. | No API operation sets font family (research/07, 09), so fonts must be inherited from masters; HTML import risks a silent Code-design fallback; Route B lands default fonts | building whole decks from blank pages |
| 4 | 2026-09-02 | **Public GitHub repo** `alderman-ai/canva-slide-layouts` hosts only placeholder-content layout HTML for Route A. **Filled presentations are never committed to the public repo** (`presentations/` is git-ignored); bundles are published only when `content_public: true` is set explicitly. | Import needs a public HTTPS URL; private content must not be published by default; Canva MCP security rule | pushing filled decks to public hosts silently |
| 5 | 2026-09-02 | **Fonts: registry, not allowlist.** Any family may be used; `spec/fonts.json` records `canva_native`, `canva_fallback`, weights; `validate` warns (not fails) on non-native. Wrap widths are computed against Inter metrics. Operator installs non-native fonts in Canva manually. | Layouts designed for non-native fonts are in scope; installing fonts is out of scope | restricting layouts to Canva-native families |
| 6 | 2026-09-02 | **Brand source is read-only.** `C:\Users\alder\Desktop\Claude Code Website\alderman-ai\` (pinned `78f20ed`) and alderman.ai are the primary brand sources; values are extracted into `spec/tokens.md` with citations; nothing is written there. | Operator designation plus the site's visitor rules | editing the site repo; adding files under its `components/` |
| 7 | 2026-09-02 | **Brand Templates are try-then-fallback.** Attempt `publish-brand-template` once per family master; on refusal, masters live in folder `FAFsWyFFv3w` and reuse goes through `copy-design` / `merge-designs`, plus UI Template links. | Pricing page says Teams/Enterprise; a Pro user was refused (research/07); yet this account holds one presentation brand template (research/09) | depending on brand templates or autofill (`autofill-design` is absent) |
| 8 | 2026-09-02 | **Ontology grounded in prior art.** Element roles reconcile OOXML and Google Slides placeholders; content units carry three fields (`slide_function`, `unit_type`, `shape`); `accepts` follows Presenton's per-layout JSON Schema with min/max items. Vocab files use SKOS-style flat keys. | research/06 | inventing an unsourced vocabulary |
| 9 | 2026-09-02 | **Custom elements are React components** in `components/slides/<Name>/` producing the four-file card convention, synced to the "alderman.ai Design System" project (`d1228f56-…`) under a new group; only new paths enter the Design Sync write set. | `/design-sync` expects a React package; the project already uses this convention (research/09) | writing `.dc.html`-only components that cannot join the DS bundle |
| 10 | 2026-09-02 | **Skills are repo-local** (`.claude/skills/{deck-decompose, element-mockup, slide-redline, slide-intake, bundle-upload}`), names provisional; `bundle-upload` is self-contained so it can be vendored into bundles. | Portability requirement: another device downloads a bundle plus the skills it needs | global-only skills |
| 11 | 2026-09-02 | **Portable bundles**: `bundles/<slug>/` is self-contained (slides, vendored layouts, deck HTML, masters HTML, ops, assets, checksums, pinned scripts and the upload skill); Canva state is per account under `canva/<account-id>.md` and git-ignored. | Operator requirement | bundles that depend on the full library or leak account ids |
| 12 | 2026-09-02 | **Model policy for execution**: orchestrator runs on **Fable 5.1 at medium effort**; subagents run on **Opus 5 at high effort**. Set `model: opus` on Agent calls and request high effort in the prompt; record deviations in WORKLOG. | Operator instruction | default subagent inheritance |
| 13 | 2026-09-02 | **Handover-first execution.** Step 0 persists all planning research verbatim into `research/`, the plan into `docs/PLAN.md`, and a boot ritual into `CLAUDE.md` before any scaffolding; every session ends with a WORKLOG entry and a CLAUDE.md status update. | Operator requirement: a fresh session must be able to execute without this context window | tacit knowledge living only in a session |
| 14 | 2026-09-02 | **Every file in the repo is indexed with a routing description** in `INDEX.md` (top level) and per-folder READMEs where a folder has many files; entries say what the file is for and when to read it. | Operator requirement for agentic retrieval | undocumented files |
| 15 | 2026-09-02 | **Naming convention for vocab ids**: archetype ids are kebab-case because they double as layout file-name stems (`L###-<archetype>.md`); all other vocab values are snake_case except `delivery_mode` and `brand`, which stay kebab (`live-talk`/`read-deck` are also type-scale modes and CSS tokens; `alderman-ai` names a directory, repo and pairing). Enforced by `spec/vocab/check-vocab.mjs`. | Two agents produced divergent ids; the taxonomy's ids were already used as file stems | mixed casing within a vocabulary |
| 16 | 2026-09-02 | **Route A promoted.** Annotated HTML import is the primary route for masters and for any filled deck whose content may be public (`content_public: true`); Route C (masters + merge + fill by locator) is the route for private content; Route B stays for elements and repairs; Route D remains a probe. Supersedes the route ordering in Decision 3. | Probe P1 showed exact geometry, editable text, preserved fonts, notes and titles from HTML (`spec/canva-limits.md` §6) | treating HTML import as unreliable |
| 17 | 2026-09-02 | **Chrome automation of Canva is a deferred last resort.** The operator reports poor results driving Canva's editor through Claude in Chrome (hundreds of screenshots, tens of hours). Nothing in the pipeline may depend on it; UI-only steps are documented as operator checklists in `spec/stack.md` and attempted by an agent only if the operator explicitly asks in that session. | Operator instruction 2026-09-02 | any plan step that requires Chrome automation to succeed |
| 18 | 2026-09-02 | **Confirmed defaults**: vault root = this directory (not a subfolder of the main Obsidian vault); public repo = `alderman-ai/canva-slide-layouts`; neutral pairing = `neutral-default` (Inter Display 600 / Inter 400); brand pairing = `alderman-ai` (Barlow / JetBrains Mono, both accessible in Canva). Skill names stay provisional until the operator renames them. | Operator confirmation 2026-09-02 | re-asking these in later sessions |
